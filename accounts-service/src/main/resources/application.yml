server:
  port: ${PORT:8100}

spring:
  jpa:
    database: MYSQL
    hibernate:
      ddl-auto: validate

  datasource:
    url: jdbc:mysql://${sqlserver:localhost}:${sqlport:3306}/demo
    username: demo_user
    password: demo_pass
    driver-class-name: com.mysql.jdbc.Driver
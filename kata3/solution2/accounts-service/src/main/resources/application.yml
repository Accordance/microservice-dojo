server:
  port: 8100

endpoints:
  shutdown:
    enabled: true

# tag::jpa[]
spring:
  jpa:
    database: MYSQL
    hibernate:
      ddl-auto: validate

  datasource:
    url: jdbc:mysql://dockerhost:3306/demo
    username: demo_user
    password: demo_pass
    driver-class-name: com.mysql.jdbc.Driver
# end::jpa[]